import React, { useState } from 'react'

interface Props {
    children: React.ReactNode
}

const AnnotherStore:React.FC<Props> = ({children}) => {
    // const [count, setCount] = useState()
  return (
    <div>annotherStore</div>
  )
}

export default AnnotherStore